#include <stdio.h>
/**
 * main - aprogramme that prints a line using printf function
 *Return: 0(executed)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
		return (0);
}
